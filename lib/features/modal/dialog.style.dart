import 'package:flutter/widgets.dart';

abstract class CDialogStyles {
  static const constraints = BoxConstraints(
    maxWidth: 400,
  );

  static const titleStyle = const TextStyle(
    fontSize: 24,
  );
}
